<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Justin Saint Assignment 2</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">

    <style>
        .user-container {
            display: flex;
            align-items: center;
            margin-bottom: 1rem;
        }

        .user-buttons {
            margin-left: 1rem;
        }
    </style>
</head>

<body>
    <%- include("svgs") %>
    <div class="container">
        <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
            <a href="/"
                class="d-flex align-items-center mb-3 mb-md-0 me-md-auto link-body-emphasis text-decoration-none">
                <svg class="bi me-2" width="40" height="32">
                    <use xlink:href="#bootstrap"></use>
                </svg>
                <span class="fs-4">Assignment 2</span>
            </a>

            <ul class="nav nav-pills">
                <% for(var i=0; i<navLinks.length; i++) { %>
                <li class="nav-item"><a href="<%= navLinks[i].link %>"
                        class="nav-link <% if (navLinks[i].link == currentURL) { %>active<% } %>"
                        aria-current="page"><%= navLinks[i].name %></a></li>
                <% } %>
            </ul>
        </header>
    </div>
    <div class="px-4 py-5 my-5 text-center" style="max-width: 1320px; margin-left: auto; margin-right: auto;">