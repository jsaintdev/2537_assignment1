<%- include("templates/header") %> 
    <% if (!emailProvided) { %>
        <h3>No email provided - try /nosql-injection?email=email@example.com</h3>
        <h3>or /nosql-injection?email[$ne]=email@example.com</h3>
    <% } else if (attackDetected) { %>
        <h1 style='color:darkred;'>A NoSQL injection attack was detected!!</h1>
    <% } else { %>
        <h1>Hello <%= result.name %></h1>
    <% } %>
<%- include("templates/footer") %> 